package mopidy

import (
	"testing"
)

func TestMopidy(t *testing.T) {
	/*
	client, err := NewClient("http://localhost:6680/mopidy/rpc")
	assert.NoError(t, err)
	
	_, _, err = client.AudiobookAvailable("Jan Tenner", "02 - Der lautlose Tod")
	assert.NoError(t, err)

	tracks, err := client.GetCurrentTracklist()
	assert.NoError(t, err)
	fmt.Println(tracks)

	track, err := client.GetCurrentTrack()
	assert.NoError(t, err)
	fmt.Println(track)

	client.Stop()
	*/
}
